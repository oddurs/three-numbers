#import "../lib/book.typ": *
#import "/build/tables/matrices.typ": generated-tables

#chapter(none)[Appendix A — The matrices]

#lead[
  Everything in this appendix is computed by the engine in this repository from
  the primaries, white points and transfer functions the standards publish. None
  of it is transcribed. If a primary in `engine/color/rgbspace.ts` changes, this
  appendix changes with it, which is the only way a reference table stays true.
]

#generated-tables

#chapter-end()
