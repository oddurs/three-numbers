Scratch documents for probing the page master. `proof.typ` is regenerated by
`node engine/cli.ts proof --figures`. Nothing here is part of the book: the
citation audit and the word count both skip this directory.
